function newArr = removeCell(arr, toRemove)
% REMOVECELL Returns all elements of 'arr' that are not 'toRemove'. 
% 'toRemove' may be one or more strings. Useful for dropping a string from
% a list of strings.

narginchk(2, 2);
if ischar(toRemove)
    toRemove = {toRemove};
end

nElts = 0;
newArr = cell(1, numel(toRemove));
for ii=1:length(
end